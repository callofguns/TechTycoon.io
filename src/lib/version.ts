/**
 * The game's current version — shown in the bottom-right corner of the app
 * and used to tag the matching GitHub release. Bump this on every shipped
 * update; per Dhruv's standing instruction, always ask what the new number
 * should be rather than picking one.
 */
export const APP_VERSION = 'v0.4.1-beta';
